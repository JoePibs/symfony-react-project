{% extends 'admin/admin.html.twig' %}

{% block title %}Editer Recette {{ recipe.title }}! ✅{% endblock %}

{% block body %}
    
    <h1>Editer Recette {{ recipe.title }}! </h1>

    {{ form_start(form)}}
        <div class="d-flex">
            {{form_row(form.title)}}
            {{form_row(form.duration)}}
        </div>
    {{ form_end(form)}}
{% endblock %}